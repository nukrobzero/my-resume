/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "drive.google.com",
          "random.imagecdn.app",
          "googleusercontent.com"
        ],
    }
}

module.exports = nextConfig